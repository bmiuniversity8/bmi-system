name: Deploy BMI System

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ─── Detect Changes ───────────────────────────────────────────────────────────
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      api: ${{ steps.filter.outputs.api }}
      portal: ${{ steps.filter.outputs.portal }}
      ums: ${{ steps.filter.outputs.ums }}
    steps:
      - uses: actions/checkout@v4
      - name: Path Filters
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            api:
              - 'apps/api/**'
              - 'packages/shared/**'
            portal:
              - 'apps/portal/**'
              - 'packages/shared/**'
            ums:
              - 'apps/ums/**'
              - 'packages/shared/**'

  # ─── Deploy Unified API Worker ────────────────────────────────────────────────
  deploy-api:
    name: Deploy API Worker
    runs-on: ubuntu-latest
    needs: changes
    if: github.event_name == 'push' && needs.changes.outputs.api == 'true'
    defaults:
      run:
        working-directory: apps/api
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies (workspace)
        run: |
          npm ci
        working-directory: .

      - name: Inject Cloudflare IDs
        run: |
          sed -i 's/${CLOUDFLARE_D1_ID}/'${{ secrets.CLOUDFLARE_D1_ID }}'/g' wrangler.jsonc

      - name: Deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: apps/api
          command: deploy

  # ─── Deploy Portal Frontend ───────────────────────────────────────────────────
  deploy-portal:
    name: Deploy Portal (Cloudflare Pages)
    runs-on: ubuntu-latest
    needs: changes
    if: github.event_name == 'push' && needs.changes.outputs.portal == 'true'
    defaults:
      run:
        working-directory: apps/portal
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies (workspace)
        run: |
          npm ci
        working-directory: .

      - name: Build portal
        run: npm run build
        env:
          NODE_ENV: production
          VITE_API_URL: "https://bmi-api.bmiuniversity107.workers.dev"

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: apps/portal
          command: pages deploy dist --project-name=bmi-portal

  # ─── Deploy UMS Frontend ──────────────────────────────────────────────────────
  deploy-ums:
    name: Deploy UMS (Cloudflare Pages)
    runs-on: ubuntu-latest
    needs: changes
    if: github.event_name == 'push' && needs.changes.outputs.ums == 'true'
    defaults:
      run:
        working-directory: apps/ums
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies (workspace)
        run: |
          npm ci
        working-directory: .

      - name: Build UMS
        run: npm run build
        env:
          NODE_ENV: production
          VITE_API_URL: "https://bmi-api.bmiuniversity107.workers.dev"

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: apps/ums
          command: pages deploy dist --project-name=bmi-ums

  # ─── Run Tests (PRs & Background) ─────────────────────────────────────────────
  test:
    name: Type Check & Test
    runs-on: ubuntu-latest
    needs: changes
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install all workspace dependencies
        run: |
          npm ci

      - name: Type check shared package
        run: npm run typecheck
        working-directory: packages/shared

      - name: Verify D1 Migrations
        run: npm run verify-migrations
        working-directory: apps/api

      - name: Type check API
        run: npm run type-check
        working-directory: apps/api
        continue-on-error: true

      - name: Run API tests (includes contract tests)
        run: npm test
        working-directory: apps/api

      - name: Type check UMS frontend
        run: npm run type-check
        working-directory: apps/ums
        continue-on-error: true

      - name: Run UMS tests
        run: npm test
        working-directory: apps/ums
        continue-on-error: true

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps
        working-directory: apps/ums

      - name: Run UMS E2E tests
        run: npm run test:e2e
        working-directory: apps/ums
        continue-on-error: true

      - name: Run npm audit
        run: npm audit --audit-level=high || true

  # ─── API Contract Tests (blocking gate) ──────────────────────────────────────
  # TODO: Re-enable when contract tests are properly configured with @cloudflare/vitest-pool-workers
  # contract-tests:
  #   name: API Contract Tests
  #   runs-on: ubuntu-latest
  #   needs: changes
  #   defaults:
  #     run:
  #       working-directory: apps/api
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '24'
  #
  #     - name: Install all workspace dependencies
  #       run: npm ci
  #       working-directory: .
  #
  #     - name: Run API contract tests
  #       # Contract tests are NOT continue-on-error — they are a hard gate.
  #       # Any regression in the { success, data: { items, page, perPage, total } }
  #       # response envelope will fail this job and block deployment.
  #       run: npx vitest run --reporter=verbose routes/ums-students.contract.test.ts routes/ums-grades.contract.test.ts routes/ums-courses.contract.test.ts

